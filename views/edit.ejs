<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-800 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/profile" class="px-3 py-2 rounded-md bg-orange-600 text-sm text-black hover:bg-orange-700">Back</a>
        </div>
        <h5 class="text-zinc-400 mb-5 text-2xl">Edit your post</h5>
        <form action="/update/<%= post._id %>" method="post" class="flex flex-col space-y-4">
            <textarea class="resize-none bg-transparent border-2 border-zinc-700 rounded-md w-1/3 p-1 outline-none" placeholder="What's on your mind" name="content"><%= post.content %></textarea>
            <input class="w-20 px-2 py-2 rounded-md text-sm cursor-pointer text-black bg-yellow-500 hover:bg-yellow-600" type="submit" value="Update">
        </form>
    </div>
</body>
</html>